# syntax=docker/dockerfile:1
FROM oven/bun:1-alpine
WORKDIR /app
# curl lets the container report health; dependencies and the Vite dev server
# are started from the compose command (source is mounted as a volume).
RUN apk add --no-cache curl
EXPOSE 3000 24678
